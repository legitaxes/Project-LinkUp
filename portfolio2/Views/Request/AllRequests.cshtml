@model IEnumerable<portfolio2.Models.RequestViewModel>
@{
    ViewData["Title"] = "My Requests";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@*<div class="table">
    <div class="container">
        @if (TempData["Message"] != null)
        {
            <h3 class="text-danger text-center">@TempData["Message"]</h3>
        }
        <div class="card text-center">
            <h5 class="card-header">All Requests</h5>
            <div class="card-body">
                <partial name="~/Views/Request/_ViewRequests.cshtml" model="@Model" />
            </div>
        </div>
    </div>
</div>*@




<div class="container">
    <div class="card">
        <h2 class="card-header text-center">All Requests</h2>
        <div class="card-body">
            <partial name="~/Views/Request/_ViewRequests.cshtml" model="@Model" />
        </div>
    </div>
</div>
