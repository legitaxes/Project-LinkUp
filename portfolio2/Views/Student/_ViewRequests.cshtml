@model IEnumerable<portfolio2.Models.RequestViewModel>

<div class="table-responsive">
    <table id="viewRequests" class="table table-striped table-hover">
        <thead class="thead-light">
            <tr>
                <th>Date</th>
                <th>Description</th>
                <th>Title</th>
                <th>Start Time</th>
                <th>End Time</th>
                <th>Points</th>
                <th>Status</th>
                <th>Location</th>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.DateRequest</td>
                    <td>@item.Description</td>
                    <td>@item.Title</td>
                    <td>@item.AvailabilityFrom</td>
                    <td>@item.AvailabilityTo</td>
                    <td>@item.PointsEarned</td>
                    <td>@item.Status</td>
                    <td>@item.LocationName</td>
                    <td>@item.Name</td>
                </tr>
            }
        </tbody>
    </table>
</div>

